- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.flake8:
    ~/.gitignore:
    ~/.gitconfig:
    ~/.zshrc:
    ~/.bashrc:
    ~/.Brewfile:
    ~/.bash_profile: bashrc
- shell:
  - [git submodule update --init --recursive, Installing submodules]
