- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.gitignore:
    ~/.gitconfig:
    ~/.zshrc:
    ~/.bashrc:
    ~/.bash_profile: bashrc
- shell:
  - [git submodule update --init --recursive, Installing submodules]
