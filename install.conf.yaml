- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.pylintrc:
    ~/.gitignore:
    ~/.gitconfig:
    ~/.zshrc:
    ~/.bashrc:
    ~/.bash_profile: bashrc
- shell:
  - [git submodule update --init --recursive, Installing submodules]
