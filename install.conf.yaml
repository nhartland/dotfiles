- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.gitignore:
    ~/.gitconfig:
- shell:
  - [git submodule update --init --recursive, Installing submodules]
