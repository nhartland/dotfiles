- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.vim: 
    ~/.vimrc: config/vimrc
    ~/.flake8: config/flake8
    ~/.gitignore: config/gitignore
    ~/.gitconfig: config/gitconfig
    ~/.zshrc: config/zshrc
    ~/.bashrc: config/bashrc
    ~/.bash_profile: config/bashrc
    ~/.skhdrc: config/skhdrc
    ~/.chunkwmrc: config/chunkwmrc
    ~/.muttrc: config/muttrc
    ~/.Brewfile: config/Brewfile
    ~/.config/ranger/rifle.conf: config/rifle
    ~/.bin/lpass-wrapper.sh: bin/lpass-wrapper.sh
    ~/Library/Preferences/kitty/kitty.conf: config/kitty
- shell:
  - [tic -x -o \~/.terminfo terminfo/kitty.terminfo, Installing kitty terminfo]
  - [git submodule update --init --recursive, Installing submodules]
